import * as React from 'react';
import { render } from 'react-dom';
import { HelloWorld } from './HelloWorld';

render(<HelloWorld />, document.querySelector('#content')); // slight diff than blog post as this sample code uses simplified MVC than using the template.